# feed-go-cli

**What** - a topic feed for Go and CLI tooling. urfave/cli, the Charm TUI stack, command-line design, terminal UX.

**Why it fits the Atmosphere** - feed generator, one of the N topic feeds in [topic-feeds](topic-feeds.md).

**Interest signal** - Kai is a urfave/cli maintainer and builds CLIs constantly (coily, the cli-* family). Deep first-hand interest.

**Keyword sketch** - `urfave/cli`, `golang cli`, `command line tool`, `charm`, `bubbletea`, `lipgloss`, `cobra`.

**Tier** - Tier 3 (high interest, but Bluesky Go-community volume is thinner than the Tier 1 and 2 topics). Candidate to fold into [agent-engineering](feed-agent-engineering.md) or run as a deliberately low-volume feed.

**Open questions**

- Is a low-volume feed worth a slot, or better merged.
