# feed-homelab-k8s

**What** - a topic feed for homelab and Kubernetes. Self-hosting, k3s, kubectl, Helm, the kai-server style single-node-plus cluster.

**Why it fits the Atmosphere** - feed generator, one of the N topic feeds in [topic-feeds](topic-feeds.md).

**Interest signal** - Kai runs k3s on kai-server and ships personal apps behind a Caddy reverse proxy on it. Kubernetes is in her role's tech stack.

**Keyword sketch** - `kubernetes`, `k3s`, `homelab`, `self-hosted`, `selfhosted`, `kubectl`, `helm`, `k8s`.

**Tier** - Tier 2 (strong interest, moderate volume). The Bluesky self-hosted community is sizable.

**Open questions**

- Whether to split homelab (hardware, self-hosting) from Kubernetes (orchestration) into two feeds if volume is high enough.
